from django.urls import path
from . import views

# this might not be necessary for now
app_name = 'ifftasks'
urlpatterns = [
    # path('', views.index, name='index'),
    # path('savetodo/', views.savetodo, name='savetodo'),
    # path('completetodo/', views.completetodo, name='completetodo'),
]
